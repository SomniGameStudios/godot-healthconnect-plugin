<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Step Sensor Permission -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>

    <application>
        <!-- Required Godot Plugin Metadata -->
        <meta-data
            android:name="org.godotengine.plugin.v2.${godotPluginName}"
            android:value="${godotPluginPackageName}.HealthConnectPlugin"/>

        <!-- Fires at exactly midnight when date changes — used for today's step baseline -->
        <receiver
            android:name=".MidnightReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.DATE_CHANGED"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>